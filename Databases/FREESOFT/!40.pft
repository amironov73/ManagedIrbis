(
  if p(v40) then
    if a(v40^a) then
      '1 Неверная структура поля: необходимо указать семейство операционных систем', /
    else
      if &uf('K40a.mnu|'v40^a)='' then 
        '2 Неизвестное семейство операционных систем ', v40^a, /
      else
        '0', /
      fi,
    fi,
  fi
)
